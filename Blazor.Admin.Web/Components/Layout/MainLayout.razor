@inherits LayoutComponentBase
    <NewLayout />
@code {
    //This page will be set as the basic page for routing access
    //Limitations include but are not limited to AuthorizeView(Authorized/NotAuthorized) usages, click event response as so forth.
    //So add a newLayout indirect implement a more comprehensive page with more features
}